openapi: "3.0.0"
info:
  version: 6.0.0
  title: Invoices
paths:
  /seller/invoices:
    post:
      operationId: SellerInvoices
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SellerInvoicesRequest"
          application/xml:
            schema:
              $ref: "#/components/schemas/SellerInvoicesRequest"
      responses:
        "200":
          description: OK
components:
  schemas:
    SellerInvoicesRequest:
      type: object
